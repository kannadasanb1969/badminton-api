export async function emit(db,n){const key=n.dedupeKey??null;await db.query(`INSERT INTO notifications (recipient_id,recipient_role,type,title,message,tournament_id,category_id,match_id,dedupe_key) SELECT $1,$2,$3,$4,$5,$6,$7,$8,$9::varchar WHERE NOT EXISTS (SELECT 1 FROM notifications WHERE recipient_id=$1 AND dedupe_key=$9::varchar)`,[n.recipientId,n.recipientRole,n.type,n.title,n.message,n.tournamentId??null,n.categoryId??null,n.matchId??null,key]);}
